{#
/**
 * @file
 * Default theme implementation to present a bicycle/in-kind donation receipt entity.
 *
 * This template is used when viewing a canonical bicycle/in-kind donation receipt page,
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 *   print a subset such as 'content.label'.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_bike_in_kind_donation()
 */
#}
<article{{ attributes }}>
  {% if view_mode != 'full' %}
    {{ title_prefix }}
    {{ title_suffix }}
  {% endif %}
  {% if content %}
    {{- content -}}
  {% endif %}
</article>
