/**
 * @file
 * Behaviors for the NBWTHEME_BS4_SUBTHEME theme.
 */

(function ($, _, Drupal) {
  Drupal.behaviors.VARTHEME_BS4_SUBTHEME = {
    attach() {
      // NBWTHEME_BS4_SUBTHEME JavaScript behaviors goes here.
    },
  };
})(window.jQuery, window._, window.Drupal);
